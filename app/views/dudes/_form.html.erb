<%= form_for(@dude) do |f| %>
  <% if @dude.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dude.errors.count, "error") %> prohibited this dude from being saved:</h2>

      <ul>
      <% @dude.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
